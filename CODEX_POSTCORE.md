# CATENA post-core Codex contract

Read these files first:

1. `docs/NEXT_ACTIONS_KO.md`
2. `docs/POSTCORE_DEPENDENCY_DAG_KO.md`
3. `docs/POSTCORE_RESEARCH_OVERVIEW_KO.md`
4. `docs/POSTCORE_CLAIM_GATES_KO.md`
5. `docs/CODEX_POSTCORE_TASKS.md`

Do not modify or reinterpret completed H1-H5 artifacts. New work begins at E10. Each experiment has one Python entry point and one YAML config. Dry-run and main runs must use different artifact runs; never overwrite a report.

The scientific priority is:

```text
learned control rank
→ representation/control co-adaptation
→ architecture-demand lattice
→ structured event-sequence bridge
→ plan-state continuation
→ official backend replication
```

Reference evidence is never promoted to official-backbone or language-model evidence.
